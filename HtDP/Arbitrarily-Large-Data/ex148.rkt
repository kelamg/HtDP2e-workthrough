;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname ex148) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))

;; Working with non-empty lists means one less test for empty lists but
;; a bit more typing than with definitions that accomodate empty lists.
;; Other than that however, one does not offer any substantial benefit
;; over the other, so no I don't think working with data definitions that
;; accomodate empty lists is better than definitions for non-empty lists.